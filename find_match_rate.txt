-- Match Rate for Left Join = 100.0 * Matching Records / Total Records 


SELECT 
    (COUNT(CASE WHEN t2.id IS NOT NULL THEN 1 END) * 1.0 / COUNT(*)) * 100 AS MatchRate
FROM table1 t1
LEFT JOIN table2 t2
ON t1.id = t2.id;